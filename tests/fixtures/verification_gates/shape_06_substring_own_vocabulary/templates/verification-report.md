# Verification Report — {Sprint}

**Legend:** `PASS` = criterion met and re-measured; `FAIL` = criterion not met; `UNCERTAIN` = could not be measured.

---

## Per-Criterion Results

| # | Criterion | Command | Measured | Expected | PASS / FAIL |
|---|-----------|---------|----------|----------|-------------|
| {n} | {criterion text} | `{command}` | {measured} | {expected} | {PASS or FAIL} |

---

## Residual Notes

State any criterion whose verdict is UNCERTAIN and why it could not be measured.
A criterion that could not be run is never recorded as PASS.

---

**Verdict:** {PASS or FAIL}
