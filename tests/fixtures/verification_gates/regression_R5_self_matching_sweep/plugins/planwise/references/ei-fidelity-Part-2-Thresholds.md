# EI Fidelity — Part 2: Thresholds

*Split from ei-fidelity.md §6 — this part carries the scaffolding-tier thresholds.*

## Thresholds

Each scaffolding tier declares the fidelity threshold its outputs must clear.
A tier that cannot state its threshold numerically has not defined one.
