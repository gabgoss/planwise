[pytest]
# Tests live OUTSIDE the shipped plugin subtree (./plugins/planwise is what the
# marketplace ships). They import the runtime modules under test from
# plugins/planwise/scripts/, so that dir is placed on sys.path here rather than
# by co-locating the tests next to the code (which would ship them).
testpaths = tests
pythonpath = plugins/planwise/scripts
